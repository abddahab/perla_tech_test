import 'dart:ui';


String arabic = "ar";
String english = "en";

Locale arabicLocal = Locale(arabic);
Locale englishLocal = Locale(english);

String path = "assets/translations";
List<Locale> supportedLocales = [Locale('en'), Locale('ar')];
